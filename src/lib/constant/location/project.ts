import { resolve } from 'path';

import { dotcorioders } from './location';

export const defaultStoragePath = resolve(dotcorioders, 'projects');
export const defaultPathsJsonFilename = `paths.json`;
